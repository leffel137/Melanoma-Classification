# SIIM-ISIC Melanoma Classification, pinned dependencies
#
# Python 3.12. The pins below were resolved together on it; nothing here needs
# 3.12 specifically, but the versions are not re-tested on anything newer.
#
#   python3.12 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Versions below were resolved and import-tested together on macOS arm64.

# --- data handling ---
pandas==3.0.5
numpy==2.5.2
scipy==1.18.0
scikit-learn==1.9.0

# --- plotting / EDA ---
matplotlib==3.11.1
seaborn==0.13.2
missingno==0.5.2

# --- image I/O and preprocessing ---
pillow==12.3.0
# "headless" is the version WITHOUT the GUI parts. We never open a window, and
# the normal opencv-python needs graphics libraries (libxcb) that a server does
# not have installed. Using headless makes the same code work on both.
opencv-python-headless==5.0.0.93

# --- modelling ---
# All modelling code is PyTorch. timm supplies the pretrained backbones
# (resnet34 for the baseline, tf_efficientnet_b4 for the final model) and
# albumentations does the augmentation.
#
# On a machine with an NVIDIA GPU, install torch from the CUDA index instead:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch==2.13.0
torchvision==0.28.0
timm==1.0.29
albumentations==2.0.8
# --- data download ---
kaggle==2.2.4

# --- tooling ---
jupyter==1.1.1
tqdm==4.70.0
PyYAML==6.0.3
